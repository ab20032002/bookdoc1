# دليل التشغيل المبسط - BookDoc Iraq

## 🚨 المشكلة الحالية
Node.js غير مثبت على جهازك، لذلك لا يمكن تشغيل المشروع.

## 🔧 الحل السريع

### الخطوة 1: تثبيت Node.js
1. **افتح المتصفح** واذهب إلى: https://nodejs.org/
2. **حمل الإصدار LTS** (الزر الأخضر)
3. **اضغط مرتين** على الملف المحمل
4. **اتبع المعالج** واضغط "Next" حتى النهاية
5. **أعد تشغيل** Command Prompt

### الخطوة 2: التحقق من التثبيت
1. **افتح Command Prompt جديد**
2. **اكتب**: `node --version`
3. **يجب أن تظهر**: `v18.17.0` (أو رقم مشابه)
4. **اكتب**: `npm --version`
5. **يجب أن تظهر**: `9.6.7` (أو رقم مشابه)

### الخطوة 3: تثبيت MongoDB
1. **افتح المتصفح** واذهب إلى: https://www.mongodb.com/try/download/community
2. **حمل MongoDB Community Server**
3. **اضغط مرتين** على الملف المحمل
4. **اختر "Complete"** للتثبيت الكامل
5. **اختر "Install MongoDB as a Service"**
6. **اضغط "Install"**

### الخطوة 4: التحقق من MongoDB
1. **افتح Command Prompt جديد**
2. **اكتب**: `mongod --version`
3. **يجب أن تظهر**: `db version v7.0.4` (أو رقم مشابه)

## 🚀 تشغيل المشروع

### الطريقة السريعة (بعد تثبيت المتطلبات):

#### 1. إنشاء مجلد المشروع
```bash
# اذهب إلى Desktop
cd Desktop

# أنشئ مجلد جديد
mkdir bookdoc-iraq

# ادخل إلى المجلد
cd bookdoc-iraq
```

#### 2. تحميل المشروع
```bash
# إذا كان المشروع على GitHub
git clone https://github.com/your-username/bookdoc-iraq.git

# أو انسخ الملفات يدوياً
```

#### 3. تشغيل MongoDB
```bash
# افتح Command Prompt جديد
mongod
```

#### 4. تشغيل الخادم الخلفي
```bash
# افتح Command Prompt جديد
cd bookdoc-iraq/backend
npm install
npm start
```

#### 5. تشغيل تطبيق المرضى
```bash
# افتح Command Prompt جديد
cd bookdoc-iraq/mobile-app
npm install
npm start
```

## 🎯 النتيجة المتوقعة

بعد تشغيل جميع الخطوات:
- **MongoDB**: يعمل على المنفذ 27017
- **الخادم الخلفي**: يعمل على http://localhost:5000
- **تطبيق المرضى**: يعمل على http://localhost:3000

## 🐛 حل المشاكل الشائعة

### مشكلة: Node.js غير مثبت
**الحل**: اتبع الخطوة 1 أعلاه

### مشكلة: MongoDB غير مثبت
**الحل**: اتبع الخطوة 3 أعلاه

### مشكلة: المنافذ محجوزة
**الحل**: 
```bash
# تحقق من المنافذ المستخدمة
netstat -ano | findstr :3000
netstat -ano | findstr :5000

# أو غير المنافذ في ملفات .env
```

### مشكلة: تبعيات مفقودة
**الحل**:
```bash
# احذف node_modules وأعد التثبيت
rm -rf node_modules
rm package-lock.json
npm install
```

## 📞 المساعدة

إذا واجهت أي مشاكل:
1. تأكد من تثبيت جميع المتطلبات
2. أعد تشغيل Command Prompt
3. تحقق من رسائل الخطأ
4. اتبع الخطوات بالترتيب

## 🎉 بعد التشغيل الناجح

ستتمكن من:
- فتح http://localhost:3000 لرؤية تطبيق المرضى
- فتح http://localhost:5000 لرؤية API
- إنشاء حسابات جديدة
- حجز مواعيد
- استكشاف جميع الميزات

---

**ملاحظة**: هذا الدليل مبسط ويركز على الحلول السريعة. اتبع الخطوات بالترتيب المذكور.
